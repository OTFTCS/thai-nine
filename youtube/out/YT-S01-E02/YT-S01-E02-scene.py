"""Auto-generated Manim scene — deterministic codegen."""

from scene_base import YouTubeScene


class YouTubeOverlay(YouTubeScene):
    """Auto-generated overlay scene."""

    def construct(self):
        self.setup()
        elapsed = 0.0

        # === Block: b-001 (hook) ===
        # Sync to 1.00s (l-0001)
        wait_gap = 1.00 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ห้องน้ำอยู่ที่ไหนคะ", lang="th", translit="hâwng náam yùu thîi nǎi khá")
        self.show_thai_line("ห้องน้ำอยู่ที่ไหนคะ", duration=3.0, fade_in=False, translit="hâwng náam yùu thîi nǎi khá")
        elapsed += 3.0

        # Sync to 4.00s (l-0002)
        wait_gap = 4.00 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_english_line("Where is the bathroom?", duration=2.0)
        elapsed += 2.0

        # Sync to 6.00s (l-0003)
        wait_gap = 6.00 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_english_line("By the end of this video, you'll know how to ask where anything is in Thai — and understand directions like 'go straight', 'turn left', 'it's near'. All the phrases you need to actually navigate Bangkok.", duration=14.21)
        elapsed += 14.21

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-002 (explain) ===
        # Sync to 20.71s (l-0004)
        wait_gap = 20.71 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("Bangkok's streets have a numbering system that confuses everyone", duration=2.476, lang="en")
        elapsed += 2.476
        self.set_subtitle("— even Thais.", lang="en")
        self.wait(2.476)
        elapsed += 2.476
        self.set_subtitle("Side streets are called soi.", lang="en")
        self.wait(2.476)
        elapsed += 2.476
        self.set_subtitle("Odd numbers on one side,", lang="en")
        self.wait(2.476)
        elapsed += 2.476
        self.set_subtitle("even on the other.", lang="en")
        self.wait(2.476)
        elapsed += 2.476
        self.set_subtitle("And if a new street gets built between Soi", lang="en")
        self.wait(2.476)
        elapsed += 2.476
        self.set_subtitle("7 and Soi 9?", lang="en")
        self.wait(2.476)
        elapsed += 2.476
        self.set_subtitle("It becomes Soi 7/1.", lang="en")
        self.wait(2.476)
        elapsed += 2.476
        self.set_subtitle("House numbers are assigned in registration order,", lang="en")
        self.wait(2.476)
        elapsed += 2.476
        self.set_subtitle("not geographic order —", lang="en")
        self.wait(2.476)
        elapsed += 2.476
        self.set_subtitle("so number 12 might be next to number 87.", lang="en")
        self.wait(2.476)
        elapsed += 2.476

        # Sync to 47.95s (l-0005)
        wait_gap = 47.95 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("In Thailand,", duration=1.971, lang="en")
        elapsed += 1.971
        self.set_subtitle("people are incredibly helpful with", lang="en")
        self.wait(1.971)
        elapsed += 1.971
        self.set_subtitle("directions.", lang="en")
        self.wait(1.971)
        elapsed += 1.971
        self.set_subtitle("If you ask,", lang="en")
        self.wait(1.971)
        elapsed += 1.971
        self.set_subtitle("most people will try their best to explain", lang="en")
        self.wait(1.971)
        elapsed += 1.971
        self.set_subtitle("— even if their English is limited.", lang="en")
        self.wait(1.971)
        elapsed += 1.971
        self.set_subtitle("So the phrase that opens everything is:", lang="en")
        self.wait(1.971)
        elapsed += 1.971

        # Sync to 61.75s (l-0006)
        wait_gap = 61.75 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ขอโทษค่ะ สถานีรถไฟฟ้าอยู่ที่ไหนคะ", lang="th", translit="khǎaw-thôot khâ sa-thǎa-nii rót-fai-fáa yùu thîi nǎi khá")
        self.show_thai_line("ขอโทษค่ะ สถานีรถไฟฟ้าอยู่ที่ไหนคะ", duration=3.0, pos="bottom", translit="khǎaw-thôot khâ sa-thǎa-nii rót-fai-fáa yùu thîi nǎi khá")
        elapsed += 3.0

        # Sync to 64.75s (l-0007)
        wait_gap = 64.75 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("Excuse me, where is the BTS station?", duration=2.648, lang="en")
        elapsed += 2.648
        self.set_subtitle("Today I'm going to teach you 8 essential", lang="en")
        self.wait(2.648)
        elapsed += 2.648
        self.set_subtitle("phrases for asking directions in Thai.", lang="en")
        self.wait(2.648)
        elapsed += 2.648
        self.set_subtitle("We'll go through each word,", lang="en")
        self.wait(2.648)
        elapsed += 2.648
        self.set_subtitle("then you'll hear them in a real conversation,", lang="en")
        self.wait(2.648)
        elapsed += 2.648
        self.set_subtitle("and finally we'll practise together.", lang="en")
        self.wait(2.648)
        elapsed += 2.648

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-003 (section-intro) ===
        # Sync to 81.14s (l-0008)
        wait_gap = 81.14 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("Let's start with the vocabulary.", lang="en")
        self.wait(2.042)
        elapsed += 2.042
        self.set_subtitle("Eight words — that's everything you need.", lang="en")
        self.wait(2.042)
        elapsed += 2.042
        self.set_subtitle("For each one I'll say the Thai,", lang="en")
        self.wait(2.042)
        elapsed += 2.042
        self.set_subtitle("explain how to use it,", lang="en")
        self.wait(2.042)
        elapsed += 2.042
        self.set_subtitle("and give you a real example.", lang="en")
        self.wait(2.042)
        elapsed += 2.042
        self.set_subtitle("Ready? Let's go.", lang="en")
        self.wait(2.042)
        elapsed += 2.042

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-004 (vocab-card) ===
        # Sync to 93.89s (l-0009)
        wait_gap = 93.89 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "อยู่ที่ไหน",
            "where is (it)?",
            "yùu thîi nǎi",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-005 (vocab-explain) ===
        # Sync to 96.89s (l-0010)
        wait_gap = 96.89 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("อยู่ที่ไหน — yùu thîi nǎi.", lang="en")
        self.wait(2.330)
        elapsed += 2.330
        self.set_subtitle("This means 'where is it?' or 'where is...?' อยู่ means 'to be located',", lang="en")
        self.wait(2.330)
        elapsed += 2.330
        self.set_subtitle("and ที่ไหน means 'where'.", lang="en")
        self.wait(2.330)
        elapsed += 2.330
        self.set_subtitle("The key thing: in Thai,", lang="en")
        self.wait(2.330)
        elapsed += 2.330
        self.set_subtitle("the question word goes at the end.", lang="en")
        self.wait(2.330)
        elapsed += 2.330
        self.set_subtitle("So instead of saying 'Where is the bathroom?',", lang="en")
        self.wait(2.330)
        elapsed += 2.330
        self.set_subtitle("you say 'Bathroom — where is it?' —", lang="en")
        self.wait(2.330)
        elapsed += 2.330
        self.set_subtitle("ห้องน้ำอยู่ที่ไหน.", lang="en")
        self.wait(2.330)
        elapsed += 2.330
        self.set_subtitle("Just put the place first,", lang="en")
        self.wait(2.330)
        elapsed += 2.330
        self.set_subtitle("then อยู่ที่ไหน at the end.", lang="en")
        self.wait(2.330)
        elapsed += 2.330
        self.set_subtitle("Works for anything.", lang="en")
        self.wait(2.330)
        elapsed += 2.330

        # Sync to 122.52s (l-0011)
        wait_gap = 122.52 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("ร้านกาแฟอยู่ที่ไหนคะ", duration=3.0, pos="bottom", translit="ráan gaa-fae yùu thîi nǎi khá")
        elapsed += 3.0

        # Sync to 125.52s (l-0012)
        wait_gap = 125.52 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'Where is the coffee shop?'", lang="en")
        self.wait(2.397)
        elapsed += 2.397
        self.set_subtitle("ร้านกาแฟ is coffee shop.", lang="en")
        self.wait(2.397)
        elapsed += 2.397
        self.set_subtitle("See how อยู่ที่ไหน comes right at the end?", lang="en")
        self.wait(2.397)
        elapsed += 2.397
        self.set_subtitle("That's the pattern every time.", lang="en")
        self.wait(2.397)
        elapsed += 2.397

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-006 (vocab-card) ===
        # Sync to 135.61s (l-0013)
        wait_gap = 135.61 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "OK, the first direction word.",
            "OK, the first direction word.",
            "",
            duration=2.53,
        )
        elapsed += 2.53

        # Sync to 138.14s (l-0014)
        wait_gap = 138.14 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "ตรงไป",
            "go straight",
            "trong bpai",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-007 (vocab-explain) ===
        # Sync to 141.14s (l-0015)
        wait_gap = 141.14 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ตรงไป — trong bpai.", lang="en")
        self.wait(2.165)
        elapsed += 2.165
        self.set_subtitle("This means 'go straight'.", lang="en")
        self.wait(2.165)
        elapsed += 2.165
        self.set_subtitle("ตรง means 'straight' and ไป means 'go'.", lang="en")
        self.wait(2.165)
        elapsed += 2.165
        self.set_subtitle("Short, easy, you'll hear it constantly.", lang="en")
        self.wait(2.165)
        elapsed += 2.165
        self.set_subtitle("People will often say it while pointing,", lang="en")
        self.wait(2.165)
        elapsed += 2.165
        self.set_subtitle("so even if they say it fast you'll know", lang="en")
        self.wait(2.165)
        elapsed += 2.165
        self.set_subtitle("what they mean.", lang="en")
        self.wait(2.165)
        elapsed += 2.165
        self.set_subtitle("ตรงไปแล้วเลี้ยวซ้าย —", lang="en")
        self.wait(2.165)
        elapsed += 2.165
        self.set_subtitle("'go straight then turn left' —", lang="en")
        self.wait(2.165)
        elapsed += 2.165
        self.set_subtitle("is probably the most common direction", lang="en")
        self.wait(2.165)
        elapsed += 2.165
        self.set_subtitle("answer in Thailand.", lang="en")
        self.wait(2.165)
        elapsed += 2.165

        # Sync to 164.95s (l-0016)
        wait_gap = 164.95 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("ตรงไปแล้วก็เห็นเลยค่ะ", duration=3.0, pos="bottom", translit="trong bpai láaew gâaw hěn loei khâ")
        elapsed += 3.0

        # Sync to 167.95s (l-0017)
        wait_gap = 167.95 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'Go straight and you'll see it right away.' เห็น is 'see',", lang="en")
        self.wait(3.197)
        elapsed += 3.197
        self.set_subtitle("เลย means 'right there'.", lang="en")
        self.wait(3.197)
        elapsed += 3.197
        self.set_subtitle("Very common reassurance when you're close.", lang="en")
        self.wait(3.197)
        elapsed += 3.197

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-008 (vocab-card) ===
        # Sync to 178.04s (l-0018)
        wait_gap = 178.04 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "Next — turn left.",
            "Next — turn left.",
            "",
            duration=2.5,
        )
        elapsed += 2.5

        # Sync to 180.54s (l-0019)
        wait_gap = 180.54 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "เลี้ยวซ้าย",
            "turn left",
            "líiaw sáai",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-009 (vocab-explain) ===
        # Sync to 183.54s (l-0020)
        wait_gap = 183.54 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("เลี้ยวซ้าย — líiaw sáai. Turn left.", lang="en")
        self.wait(3.301)
        elapsed += 3.301
        self.set_subtitle("เลี้ยว means 'turn', ซ้าย means 'left'.", lang="en")
        self.wait(3.301)
        elapsed += 3.301
        self.set_subtitle("Falling tone on ซ้าย — your voice drops.", lang="en")
        self.wait(3.301)
        elapsed += 3.301
        self.set_subtitle("I always tell my students: practise ซ้าย and ขวา together as a pair,", lang="en")
        self.wait(3.301)
        elapsed += 3.301
        self.set_subtitle("with the gesture, until they're automatic.", lang="en")
        self.wait(3.301)
        elapsed += 3.301
        self.set_subtitle("If you get them wrong you'll go the wrong direction", lang="en")
        self.wait(3.301)
        elapsed += 3.301
        self.set_subtitle("— which I've seen happen to many students.", lang="en")
        self.wait(3.301)
        elapsed += 3.301

        # Sync to 206.65s (l-0021)
        wait_gap = 206.65 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("เลี้ยวซ้ายตรงสี่แยกครับ", duration=3.0, pos="bottom", translit="líiaw sáai trong sìi-yâaek khráp")
        elapsed += 3.0

        # Sync to 209.65s (l-0022)
        wait_gap = 209.65 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'Turn left at the intersection.' สี่แยก means 'four-way intersection'", lang="en")
        self.wait(2.817)
        elapsed += 2.817
        self.set_subtitle("— สี่ is four, แยก is split.", lang="en")
        self.wait(2.817)
        elapsed += 2.817
        self.set_subtitle("You'll hear this landmark constantly in", lang="en")
        self.wait(2.817)
        elapsed += 2.817
        self.set_subtitle("directions.", lang="en")
        self.wait(2.817)
        elapsed += 2.817

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-010 (vocab-card) ===
        # Sync to 221.42s (l-0023)
        wait_gap = 221.42 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "And the opposite — turn right.",
            "And the opposite — turn right.",
            "",
            duration=2.6,
        )
        elapsed += 2.6

        # Sync to 224.02s (l-0024)
        wait_gap = 224.02 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "เลี้ยวขวา",
            "turn right",
            "líiaw khwǎa",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-011 (vocab-explain) ===
        # Sync to 227.02s (l-0025)
        wait_gap = 227.02 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("เลี้ยวขวา — líiaw khwǎa. Turn right.", lang="en")
        self.wait(2.296)
        elapsed += 2.296
        self.set_subtitle("ขวา has a rising tone — voice goes up.", lang="en")
        self.wait(2.296)
        elapsed += 2.296
        self.set_subtitle("Compare: ซ้าย falls, ขวา rises.", lang="en")
        self.wait(2.296)
        elapsed += 2.296
        self.set_subtitle("That tonal contrast is how you tell them", lang="en")
        self.wait(2.296)
        elapsed += 2.296
        self.set_subtitle("apart.", lang="en")
        self.wait(2.296)
        elapsed += 2.296
        self.set_subtitle("If you can nail those two tones you'll", lang="en")
        self.wait(2.296)
        elapsed += 2.296
        self.set_subtitle("always go the right way.", lang="en")
        self.wait(2.296)
        elapsed += 2.296
        self.set_subtitle("ขวา also means 'the right side' on its own", lang="en")
        self.wait(2.296)
        elapsed += 2.296
        self.set_subtitle("— you'll see it on signs.", lang="en")
        self.wait(2.296)
        elapsed += 2.296

        # Sync to 247.68s (l-0026)
        wait_gap = 247.68 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("เลี้ยวขวาแล้วตรงไปเลยค่ะ", duration=3.0, pos="bottom", translit="líiaw khwǎa láaew trong bpai loei khâ")
        elapsed += 3.0

        # Sync to 250.68s (l-0027)
        wait_gap = 250.68 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'Turn right and then just go straight.' แล้ว means 'then',", lang="en")
        self.wait(3.080)
        elapsed += 3.080
        self.set_subtitle("เลย makes it sound natural —", lang="en")
        self.wait(3.080)
        elapsed += 3.080
        self.set_subtitle("'just go right ahead from there'.", lang="en")
        self.wait(3.080)
        elapsed += 3.080

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-012 (vocab-card) ===
        # Sync to 260.42s (l-0028)
        wait_gap = 260.42 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "OK, next — near.",
            "OK, next — near.",
            "",
            duration=2.5,
        )
        elapsed += 2.5

        # Sync to 262.92s (l-0029)
        wait_gap = 262.92 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "ใกล้",
            "near / close",
            "glâi",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-013 (vocab-explain) ===
        # Sync to 265.92s (l-0030)
        wait_gap = 265.92 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ใกล้ — glâi. This means 'near' or 'close'.", lang="en")
        self.wait(2.342)
        elapsed += 2.342
        self.set_subtitle("Falling tone — voice goes down.", lang="en")
        self.wait(2.342)
        elapsed += 2.342
        self.set_subtitle("After you get directions,", lang="en")
        self.wait(2.342)
        elapsed += 2.342
        self.set_subtitle("the natural follow-up question is ใกล้ไหมคะ", lang="en")
        self.wait(2.342)
        elapsed += 2.342
        self.set_subtitle("— 'Is it near?' —", lang="en")
        self.wait(2.342)
        elapsed += 2.342
        self.set_subtitle("and if they say ใกล้ค่ะ you can walk,", lang="en")
        self.wait(2.342)
        elapsed += 2.342
        self.set_subtitle("if not you might need a taxi.", lang="en")
        self.wait(2.342)
        elapsed += 2.342
        self.set_subtitle("Really useful short question that Thai", lang="en")
        self.wait(2.342)
        elapsed += 2.342
        self.set_subtitle("people use all the time.", lang="en")
        self.wait(2.342)
        elapsed += 2.342

        # Sync to 287.00s (l-0031)
        wait_gap = 287.00 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("ใกล้แค่นี้เองค่ะ", duration=3.0, pos="bottom", translit="glâi khâe níi eeng khâ")
        elapsed += 3.0

        # Sync to 290.00s (l-0032)
        wait_gap = 290.00 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'It's only this close.' แค่นี้เอง is a really natural Thai expression", lang="en")
        self.wait(2.590)
        elapsed += 2.590
        self.set_subtitle("— 'just this much'.", lang="en")
        self.wait(2.590)
        elapsed += 2.590
        self.set_subtitle("It's the kind of thing someone says to", lang="en")
        self.wait(2.590)
        elapsed += 2.590
        self.set_subtitle("reassure you that you haven't walked too", lang="en")
        self.wait(2.590)
        elapsed += 2.590
        self.set_subtitle("far.", lang="en")
        self.wait(2.590)
        elapsed += 2.590

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-014 (vocab-card) ===
        # Sync to 303.45s (l-0033)
        wait_gap = 303.45 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "And the opposite — far.",
            "And the opposite — far.",
            "",
            duration=2.5,
        )
        elapsed += 2.5

        # Sync to 305.95s (l-0034)
        wait_gap = 305.95 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "ไกล",
            "far",
            "glai",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-015 (vocab-explain) ===
        # Sync to 308.95s (l-0035)
        wait_gap = 308.95 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ไกล — glai. This means 'far'.", lang="en")
        self.wait(1.973)
        elapsed += 1.973
        self.set_subtitle("Mid tone — flat.", lang="en")
        self.wait(1.973)
        elapsed += 1.973
        self.set_subtitle("Compare with ใกล้ which falls.", lang="en")
        self.wait(1.973)
        elapsed += 1.973
        self.set_subtitle("They look almost identical on paper —", lang="en")
        self.wait(1.973)
        elapsed += 1.973
        self.set_subtitle("ใกล้ and ไกล —", lang="en")
        self.wait(1.973)
        elapsed += 1.973
        self.set_subtitle("but the tone makes all the difference.", lang="en")
        self.wait(1.973)
        elapsed += 1.973
        self.set_subtitle("This is one of those pairs you just have", lang="en")
        self.wait(1.973)
        elapsed += 1.973
        self.set_subtitle("to drill.", lang="en")
        self.wait(1.973)
        elapsed += 1.973
        self.set_subtitle("A warning: when someone says ไกลนิดหน่อย —", lang="en")
        self.wait(1.973)
        elapsed += 1.973
        self.set_subtitle("'a little far' —", lang="en")
        self.wait(1.973)
        elapsed += 1.973
        self.set_subtitle("it usually means very far.", lang="en")
        self.wait(1.973)
        elapsed += 1.973
        self.set_subtitle("Thai people are polite.", lang="en")
        self.wait(1.973)
        elapsed += 1.973

        # Sync to 332.62s (l-0036)
        wait_gap = 332.62 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("ไกลมากไหมคะ", duration=3.0, pos="bottom", translit="glai mâak mǎi khá")
        elapsed += 3.0

        # Sync to 335.62s (l-0037)
        wait_gap = 335.62 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'Is it very far?' มาก adds 'very',", lang="en")
        self.wait(2.433)
        elapsed += 2.433
        self.set_subtitle("ไหม makes it a question.", lang="en")
        self.wait(2.433)
        elapsed += 2.433
        self.set_subtitle("Useful when you're on foot and wondering", lang="en")
        self.wait(2.433)
        elapsed += 2.433
        self.set_subtitle("whether you need transport.", lang="en")
        self.wait(2.433)
        elapsed += 2.433

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-016 (vocab-card) ===
        # Sync to 345.85s (l-0038)
        wait_gap = 345.85 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "Now — probably the most urgent direction question you'll ever ask.",
            "Now — probably the most urgent direction question you'll ever ask.",
            "",
            duration=5.12,
        )
        elapsed += 5.12

        # Sync to 350.97s (l-0039)
        wait_gap = 350.97 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "ห้องน้ำ",
            "bathroom / toilet",
            "hâwng náam",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-017 (vocab-explain) ===
        # Sync to 353.97s (l-0040)
        wait_gap = 353.97 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ห้องน้ำ — hâwng náam.", lang="en")
        self.wait(1.967)
        elapsed += 1.967
        self.set_subtitle("This literally means 'water room' —", lang="en")
        self.wait(1.967)
        elapsed += 1.967
        self.set_subtitle("ห้อง is room, น้ำ is water.", lang="en")
        self.wait(1.967)
        elapsed += 1.967
        self.set_subtitle("It's the everyday word for bathroom or", lang="en")
        self.wait(1.967)
        elapsed += 1.967
        self.set_subtitle("toilet.", lang="en")
        self.wait(1.967)
        elapsed += 1.967
        self.set_subtitle("You'll see it on signs everywhere —", lang="en")
        self.wait(1.967)
        elapsed += 1.967
        self.set_subtitle("shopping malls, restaurants,", lang="en")
        self.wait(1.967)
        elapsed += 1.967
        self.set_subtitle("petrol stations.", lang="en")
        self.wait(1.967)
        elapsed += 1.967
        self.set_subtitle("Just add อยู่ที่ไหน after it and you have", lang="en")
        self.wait(1.967)
        elapsed += 1.967
        self.set_subtitle("the most-used direction question in", lang="en")
        self.wait(1.967)
        elapsed += 1.967
        self.set_subtitle("Thailand.", lang="en")
        self.wait(1.967)
        elapsed += 1.967

        # Sync to 375.61s (l-0041)
        wait_gap = 375.61 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("ขอถามหน่อยได้ไหมคะ ห้องน้ำอยู่แถวไหนคะ", duration=3.0, pos="bottom", translit="khǎaw thǎam nàuy dâai mǎi khá hâwng náam yùu thǎaew nǎi khá")
        elapsed += 3.0

        # Sync to 378.61s (l-0042)
        wait_gap = 378.61 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'Excuse me,", lang="en")
        self.wait(2.268)
        elapsed += 2.268
        self.set_subtitle("which area is the bathroom in?' อยู่แถวไหน is a softer version", lang="en")
        self.wait(2.268)
        elapsed += 2.268
        self.set_subtitle("— แถว means 'area around'.", lang="en")
        self.wait(2.268)
        elapsed += 2.268
        self.set_subtitle("More casual than อยู่ที่ไหน.", lang="en")
        self.wait(2.268)
        elapsed += 2.268
        self.set_subtitle("Both work perfectly.", lang="en")
        self.wait(2.268)
        elapsed += 2.268

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-018 (vocab-card) ===
        # Sync to 390.45s (l-0043)
        wait_gap = 390.45 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "And the last one — this one is your lifeline in Bangkok.",
            "And the last one — this one is your lifeline in Bangkok.",
            "",
            duration=4.42,
        )
        elapsed += 4.42

        # Sync to 394.87s (l-0044)
        wait_gap = 394.87 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "สถานีรถไฟฟ้า",
            "BTS station",
            "sa-thǎa-nii rót-fai-fáa",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-019 (vocab-explain) ===
        # Sync to 397.87s (l-0045)
        wait_gap = 397.87 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("สถานีรถไฟฟ้า — sa-thǎa-nii rót-fai-fáa.", lang="en")
        self.wait(2.078)
        elapsed += 2.078
        self.set_subtitle("BTS Skytrain station.", lang="en")
        self.wait(2.078)
        elapsed += 2.078
        self.set_subtitle("Break it down: สถานี is station,", lang="en")
        self.wait(2.078)
        elapsed += 2.078
        self.set_subtitle("รถ is vehicle, ไฟฟ้า is electric —", lang="en")
        self.wait(2.078)
        elapsed += 2.078
        self.set_subtitle("so it's literally 'electric train", lang="en")
        self.wait(2.078)
        elapsed += 2.078
        self.set_subtitle("station'.", lang="en")
        self.wait(2.078)
        elapsed += 2.078
        self.set_subtitle("The BTS is the easiest way to get around", lang="en")
        self.wait(2.078)
        elapsed += 2.078
        self.set_subtitle("Bangkok and stations are visible landmarks", lang="en")
        self.wait(2.078)
        elapsed += 2.078
        self.set_subtitle("on every main road.", lang="en")
        self.wait(2.078)
        elapsed += 2.078
        self.set_subtitle("If you're lost,", lang="en")
        self.wait(2.078)
        elapsed += 2.078
        self.set_subtitle("asking for the nearest station gets you to", lang="en")
        self.wait(2.078)
        elapsed += 2.078
        self.set_subtitle("safety fast.", lang="en")
        self.wait(2.078)
        elapsed += 2.078

        # Sync to 422.80s (l-0046)
        wait_gap = 422.80 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("สถานีรถไฟฟ้าอยู่ใกล้ไหมคะ", duration=3.0, pos="bottom", translit="sa-thǎa-nii rót-fai-fáa yùu glâi mǎi khá")
        elapsed += 3.0

        # Sync to 425.80s (l-0047)
        wait_gap = 425.80 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'Is the BTS station nearby?' Notice how this combines สถานีรถไฟฟ้า,", lang="en")
        self.wait(2.275)
        elapsed += 2.275
        self.set_subtitle("อยู่, and ใกล้ไหม —", lang="en")
        self.wait(2.275)
        elapsed += 2.275
        self.set_subtitle("three of your eight words working", lang="en")
        self.wait(2.275)
        elapsed += 2.275
        self.set_subtitle("together.", lang="en")
        self.wait(2.275)
        elapsed += 2.275
        self.set_subtitle("That's exactly how it sounds in a real", lang="en")
        self.wait(2.275)
        elapsed += 2.275
        self.set_subtitle("conversation.", lang="en")
        self.wait(2.275)
        elapsed += 2.275

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-020 (section-intro) ===
        # Sync to 439.95s (l-0048)
        wait_gap = 439.95 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("OK — you know all eight phrases.", lang="en")
        self.wait(2.075)
        elapsed += 2.075
        self.set_subtitle("Now I'm going to put them into a short", lang="en")
        self.wait(2.075)
        elapsed += 2.075
        self.set_subtitle("street conversation.", lang="en")
        self.wait(2.075)
        elapsed += 2.075
        self.set_subtitle("Imagine you've just come out of a shopping mall,", lang="en")
        self.wait(2.075)
        elapsed += 2.075
        self.set_subtitle("you're trying to find the BTS station.", lang="en")
        self.wait(2.075)
        elapsed += 2.075
        self.set_subtitle("I'll speak at natural speed,", lang="en")
        self.wait(2.075)
        elapsed += 2.075
        self.set_subtitle("the way a Thai person would really talk.", lang="en")
        self.wait(2.075)
        elapsed += 2.075
        self.set_subtitle("Don't pause the video —", lang="en")
        self.wait(2.075)
        elapsed += 2.075
        self.set_subtitle("just listen and see how much you can", lang="en")
        self.wait(2.075)
        elapsed += 2.075
        self.set_subtitle("follow.", lang="en")
        self.wait(2.075)
        elapsed += 2.075
        self.set_subtitle("Ready?", lang="en")
        self.wait(2.075)
        elapsed += 2.075

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-021 (natural-listen) ===
        # Sync to 463.27s (l-0049)
        wait_gap = 463.27 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_accumulate("ขอโทษค่ะ สถานีรถไฟฟ้าอยู่ที่ไหนคะ", duration=3.0, translit="khǎaw-thôot khâ sa-thǎa-nii rót-fai-fáa yùu thîi nǎi khá")
        elapsed += 3.0

        # Sync to 466.27s (l-0050)
        wait_gap = 466.27 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_accumulate("ตรงไปแล้วเลี้ยวซ้ายค่ะ", duration=3.0, translit="trong bpai láaew líiaw sáai khâ")
        elapsed += 3.0

        # Sync to 469.27s (l-0051)
        wait_gap = 469.27 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_accumulate("ใกล้ไหมคะ", duration=3.0, translit="glâi mǎi khá")
        elapsed += 3.0

        # Sync to 472.27s (l-0052)
        wait_gap = 472.27 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_accumulate("ใกล้ค่ะ ห้านาทีค่ะ", duration=3.0, translit="glâi khâ hâa naa-thii khâ")
        elapsed += 3.0

        # Sync to 475.27s (l-0053)
        wait_gap = 475.27 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_accumulate("ขอบคุณค่ะ", duration=2.5, translit="khàawp-khun khâ")
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-022 (explain) ===
        # Sync to 478.27s (l-0054)
        wait_gap = 478.27 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("How was that?", duration=2.030, lang="en")
        elapsed += 2.030
        self.set_subtitle("Could you catch the key phrases?", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("Don't worry if it went fast —", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("that's exactly what real Thai sounds like.", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("Now let's slow it down and go through it", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("line by line.", lang="en")
        self.wait(2.030)
        elapsed += 2.030

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-023 (section-intro) ===
        # Sync to 490.95s (l-0055)
        wait_gap = 490.95 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("Same conversation,", lang="en")
        self.wait(1.820)
        elapsed += 1.820
        self.set_subtitle("but this time I'll say each line, pause,", lang="en")
        self.wait(1.820)
        elapsed += 1.820
        self.set_subtitle("and break down exactly what each part", lang="en")
        self.wait(1.820)
        elapsed += 1.820
        self.set_subtitle("means.", lang="en")
        self.wait(1.820)
        elapsed += 1.820

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-024 (breakdown) ===
        # Sync to 498.73s (l-0056)
        wait_gap = 498.73 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_breakdown_triplet(
            "ขอโทษค่ะ สถานีรถไฟฟ้าอยู่ที่ไหนคะ",
            "khǎaw-thôot khâ sa-thǎa-nii rót-fai-fáa yùu thîi nǎi khá",
            "Excuse me, where is the BTS station?",
            duration=6.5,
            translit_delay=3.0,
            english_delay=5.0,
        )
        elapsed += 6.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-025 (explain) ===
        # Sync to 505.73s (l-0059)
        wait_gap = 505.73 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("ขอโทษค่ะ is 'excuse me' —", duration=2.318, lang="en")
        elapsed += 2.318
        self.set_subtitle("a polite opener before asking a stranger", lang="en")
        self.wait(2.318)
        elapsed += 2.318
        self.set_subtitle("anything.", lang="en")
        self.wait(2.318)
        elapsed += 2.318
        self.set_subtitle("Then สถานีรถไฟฟ้า is the BTS station.", lang="en")
        self.wait(2.318)
        elapsed += 2.318
        self.set_subtitle("อยู่ที่ไหน — where is it?", lang="en")
        self.wait(2.318)
        elapsed += 2.318
        self.set_subtitle("คะ is the soft polite particle for women.", lang="en")
        self.wait(2.318)
        elapsed += 2.318
        self.set_subtitle("Notice how the question word ที่ไหน comes right at the end", lang="en")
        self.wait(2.318)
        elapsed += 2.318
        self.set_subtitle("— place first, then 'where' at the end.", lang="en")
        self.wait(2.318)
        elapsed += 2.318
        self.set_subtitle("That's the rule.", lang="en")
        self.wait(2.318)
        elapsed += 2.318

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-026 (breakdown) ===
        # Sync to 527.09s (l-0060)
        wait_gap = 527.09 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_breakdown_triplet(
            "ตรงไปแล้วเลี้ยวซ้ายค่ะ",
            "trong bpai láaew líiaw sáai khâ",
            "Go straight then turn left.",
            duration=6.5,
            translit_delay=3.0,
            english_delay=5.0,
        )
        elapsed += 6.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-027 (explain) ===
        # Sync to 534.09s (l-0063)
        wait_gap = 534.09 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("ตรงไป — go straight. แล้ว — then.", duration=2.240, lang="en")
        elapsed += 2.240
        self.set_subtitle("เลี้ยวซ้าย — turn left.", lang="en")
        self.wait(2.240)
        elapsed += 2.240
        self.set_subtitle("The structure in Thai is the same as English here: action,", lang="en")
        self.wait(2.240)
        elapsed += 2.240
        self.set_subtitle("then next action.", lang="en")
        self.wait(2.240)
        elapsed += 2.240
        self.set_subtitle("แล้ว is your 'then' connector.", lang="en")
        self.wait(2.240)
        elapsed += 2.240
        self.set_subtitle("Very simple, very logical.", lang="en")
        self.wait(2.240)
        elapsed += 2.240

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-028 (breakdown) ===
        # Sync to 548.03s (l-0064)
        wait_gap = 548.03 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_breakdown_triplet(
            "ใกล้ไหมคะ",
            "glâi mǎi khá",
            "Is it near?",
            duration=6.5,
            translit_delay=3.0,
            english_delay=5.0,
        )
        elapsed += 6.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-029 (explain) ===
        # Sync to 555.03s (l-0067)
        wait_gap = 555.03 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("ใกล้ is 'near'.", duration=2.030, lang="en")
        elapsed += 2.030
        self.set_subtitle("ไหม turns it into a yes/no question —", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("it's the Thai question particle.", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("So ใกล้ไหม is literally 'near?' —", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("that's all you need.", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("Super short, completely natural.", lang="en")
        self.wait(2.030)
        elapsed += 2.030

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-030 (breakdown) ===
        # Sync to 567.71s (l-0068)
        wait_gap = 567.71 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_breakdown_triplet(
            "ใกล้ค่ะ ห้านาทีค่ะ",
            "glâi khâ hâa naa-thii khâ",
            "It's close. Five minutes.",
            duration=6.5,
            translit_delay=3.0,
            english_delay=5.0,
        )
        elapsed += 6.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-031 (explain) ===
        # Sync to 574.71s (l-0071)
        wait_gap = 574.71 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("ใกล้ค่ะ — it's close.", duration=2.497, lang="en")
        elapsed += 2.497
        self.set_subtitle("ห้า is five, นาที is minutes.", lang="en")
        self.wait(2.497)
        elapsed += 2.497
        self.set_subtitle("Thais often answer distance questions in walking time rather than metres", lang="en")
        self.wait(2.497)
        elapsed += 2.497
        self.set_subtitle("— which is actually more useful.", lang="en")
        self.wait(2.497)
        elapsed += 2.497
        self.set_subtitle("ห้านาที means you can walk.", lang="en")
        self.wait(2.497)
        elapsed += 2.497
        self.set_subtitle("ยี่สิบนาที means get a taxi.", lang="en")
        self.wait(2.497)
        elapsed += 2.497

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-032 (section-intro) ===
        # Sync to 590.19s (l-0072)
        wait_gap = 590.19 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("Now let's practise.", lang="en")
        self.wait(1.552)
        elapsed += 1.552
        self.set_subtitle("I'm going to give you a situation,", lang="en")
        self.wait(1.552)
        elapsed += 1.552
        self.set_subtitle("you say the Thai.", lang="en")
        self.wait(1.552)
        elapsed += 1.552
        self.set_subtitle("I'll wait —", lang="en")
        self.wait(1.552)
        elapsed += 1.552
        self.set_subtitle("actually try to say it before I give the", lang="en")
        self.wait(1.552)
        elapsed += 1.552
        self.set_subtitle("answer.", lang="en")
        self.wait(1.552)
        elapsed += 1.552

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-033 (drill-prompt) ===
        # Sync to 600.00s (l-0073)
        wait_gap = 600.00 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_drill_prompt(
            "How would you ask: 'Where is the bathroom?'",
            duration=5.01,
            try_delay=3.51,
        )
        elapsed += 5.01

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-034 (drill-answer) ===
        # Sync to 605.51s (l-0075)
        wait_gap = 605.51 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ห้องน้ำอยู่ที่ไหนคะ", lang="th", translit="hâwng náam yùu thîi nǎi khá")
        self.show_thai_line("ห้องน้ำอยู่ที่ไหนคะ", duration=3.0, fade_in=False, translit="hâwng náam yùu thîi nǎi khá")
        elapsed += 3.0

        # Sync to 608.51s (l-0076)
        wait_gap = 608.51 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That's right —", lang="en")
        self.wait(1.47)
        elapsed += 1.47
        self.set_subtitle("ห้องน้ำอยู่ที่ไหน means 'where is the", lang="en")
        self.wait(1.47)
        elapsed += 1.47
        self.set_subtitle("bathroom?'", lang="en")
        self.wait(1.47)
        elapsed += 1.47

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-035 (drill-prompt) ===
        # Sync to 613.42s (l-0077)
        wait_gap = 613.42 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_drill_prompt(
            "Someone gives you directions: 'Go straight then turn right.' How do you say that in Thai?",
            duration=8.23,
            try_delay=6.73,
        )
        elapsed += 8.23

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-036 (drill-answer) ===
        # Sync to 622.15s (l-0079)
        wait_gap = 622.15 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ตรงไปแล้วเลี้ยวขวาค่ะ", lang="th", translit="trong bpai láaew líiaw khwǎa khâ")
        self.show_thai_line("ตรงไปแล้วเลี้ยวขวาค่ะ", duration=3.0, fade_in=False, translit="trong bpai láaew líiaw khwǎa khâ")
        elapsed += 3.0

        # Sync to 625.15s (l-0080)
        wait_gap = 625.15 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That's right —", lang="en")
        self.wait(1.633)
        elapsed += 1.633
        self.set_subtitle("ตรงไปแล้วเลี้ยวขวา means 'go straight then", lang="en")
        self.wait(1.633)
        elapsed += 1.633
        self.set_subtitle("turn right'.", lang="en")
        self.wait(1.633)
        elapsed += 1.633

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-037 (drill-prompt) ===
        # Sync to 630.55s (l-0081)
        wait_gap = 630.55 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_drill_prompt(
            "You've got directions. You want to check: is it near? How do you ask that?",
            duration=7.18,
            try_delay=5.68,
        )
        elapsed += 7.18

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-038 (drill-answer) ===
        # Sync to 638.23s (l-0083)
        wait_gap = 638.23 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ใกล้ไหมคะ", lang="th", translit="glâi mǎi khá")
        self.show_thai_line("ใกล้ไหมคะ", duration=3.0, fade_in=False, translit="glâi mǎi khá")
        elapsed += 3.0

        # Sync to 641.23s (l-0084)
        wait_gap = 641.23 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That's right — ใกล้ไหม means 'is it near?'", lang="en")
        self.wait(2.94)
        elapsed += 2.94

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-039 (section-intro) ===
        # Sync to 644.67s (l-0085)
        wait_gap = 644.67 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("One more section — shadowing.", lang="en")
        self.wait(1.960)
        elapsed += 1.960
        self.set_subtitle("I'll say each phrase slowly.", lang="en")
        self.wait(1.960)
        elapsed += 1.960
        self.set_subtitle("Repeat after me.", lang="en")
        self.wait(1.960)
        elapsed += 1.960
        self.set_subtitle("Really say it out loud —", lang="en")
        self.wait(1.960)
        elapsed += 1.960
        self.set_subtitle("this is where pronunciation gets fixed.", lang="en")
        self.wait(1.960)
        elapsed += 1.960

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-040 (shadowing) ===
        # Sync to 654.97s (l-0086)
        wait_gap = 654.97 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ห้อง น้ำ อยู่ ที่ไหน คะ", lang="th", translit="hâwng náam yùu thîi nǎi khá")
        self.show_shadowing_line("ห้อง น้ำ อยู่ ที่ไหน คะ", duration=3.0, highlight=True, translit="hâwng náam yùu thîi nǎi khá")
        elapsed += 3.0

        # Sync to 657.97s (l-0087)
        wait_gap = 657.97 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ตรง ไป แล้ว เลี้ยว ซ้าย ค่ะ", lang="th", translit="trong bpai láaew líiaw sáai khâ")
        self.show_shadowing_line("ตรง ไป แล้ว เลี้ยว ซ้าย ค่ะ", duration=3.0, highlight=True, translit="trong bpai láaew líiaw sáai khâ")
        elapsed += 3.0

        # Sync to 660.97s (l-0088)
        wait_gap = 660.97 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ตรง ไป แล้ว เลี้ยว ขวา ค่ะ", lang="th", translit="trong bpai láaew líiaw khwǎa khâ")
        self.show_shadowing_line("ตรง ไป แล้ว เลี้ยว ขวา ค่ะ", duration=3.0, highlight=True, translit="trong bpai láaew líiaw khwǎa khâ")
        elapsed += 3.0

        # Sync to 663.97s (l-0089)
        wait_gap = 663.97 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ใกล้ ไหม คะ", lang="th", translit="glâi mǎi khá")
        self.show_shadowing_line("ใกล้ ไหม คะ", duration=3.0, highlight=True, translit="glâi mǎi khá")
        elapsed += 3.0

        # Sync to 666.97s (l-0090)
        wait_gap = 666.97 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ใกล้ ค่ะ ห้า นาที ค่ะ", lang="th", translit="glâi khâ hâa naa-thii khâ")
        self.show_shadowing_line("ใกล้ ค่ะ ห้า นาที ค่ะ", duration=3.0, highlight=True, translit="glâi khâ hâa naa-thii khâ")
        elapsed += 3.0

        # Sync to 669.97s (l-0091)
        wait_gap = 669.97 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ขอบ คุณ ค่ะ", lang="th", translit="khàawp-khun khâ")
        self.show_shadowing_line("ขอบ คุณ ค่ะ", duration=2.5, highlight=True, translit="khàawp-khun khâ")
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-041 (recap) ===
        # Sync to 672.97s (l-0092)
        wait_gap = 672.97 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_english_line("Quick recap — here are your 8 direction phrases in Thai.", duration=3.92)
        elapsed += 3.92

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-042 (teaser) ===
        # Sync to 677.39s (l-0093)
        wait_gap = 677.39 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_english_line("Next time — taking a taxi. You'll learn how to tell the driver where to go, ask them to use the meter, and say 'stop here'. See you then.", duration=10.09)
        elapsed += 10.09

        # Sync to 687.48s (l-0094)
        wait_gap = 687.48 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("เปิดมิเตอร์ด้วยค่ะ", lang="th", translit="bpèrt mí-dtêr dûuay khâ")
        self.show_thai_line("เปิดมิเตอร์ด้วยค่ะ", duration=2.5, fade_in=True, translit="bpèrt mí-dtêr dûuay khâ")
        elapsed += 2.5

