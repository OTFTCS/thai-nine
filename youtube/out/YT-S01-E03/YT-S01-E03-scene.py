"""Auto-generated Manim scene — deterministic codegen."""

from scene_base import YouTubeScene


class YouTubeOverlay(YouTubeScene):
    """Auto-generated overlay scene."""

    def construct(self):
        self.setup()
        elapsed = 0.0

        # === Block: b-001 (hook) ===
        # Sync to 1.00s (l-0001)
        wait_gap = 1.00 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ไปสยามค่ะ", lang="th", translit="bpai sa-yǎam khâ")
        self.show_thai_line("ไปสยามค่ะ", duration=3.0, fade_in=False, translit="bpai sa-yǎam khâ")
        elapsed += 3.0

        # Sync to 4.00s (l-0002)
        wait_gap = 4.00 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_english_line("Go to Siam, please.", duration=2.0)
        elapsed += 2.0

        # Sync to 6.00s (l-0003)
        wait_gap = 6.00 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_english_line("By the end of this video, you'll know how to take a taxi anywhere in Bangkok — where to go, use the meter, direct the driver, and pay the right price. All in Thai.", duration=11.41)
        elapsed += 11.41

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-002 (explain) ===
        # Sync to 17.91s (l-0004)
        wait_gap = 17.91 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("Bangkok has over 100,000 taxis —", duration=2.198, lang="en")
        elapsed += 2.198
        self.set_subtitle("and the colour of the car actually means", lang="en")
        self.wait(2.198)
        elapsed += 2.198
        self.set_subtitle("something.", lang="en")
        self.wait(2.198)
        elapsed += 2.198
        self.set_subtitle("Yellow-green means privately owned by the", lang="en")
        self.wait(2.198)
        elapsed += 2.198
        self.set_subtitle("driver.", lang="en")
        self.wait(2.198)
        elapsed += 2.198
        self.set_subtitle("Red-blue means the driver rents the car.", lang="en")
        self.wait(2.198)
        elapsed += 2.198
        self.set_subtitle("By law, every taxi has to use the meter —", lang="en")
        self.wait(2.198)
        elapsed += 2.198
        self.set_subtitle("the starting fare is 35 baht,", lang="en")
        self.wait(2.198)
        elapsed += 2.198
        self.set_subtitle("about one US dollar.", lang="en")
        self.wait(2.198)
        elapsed += 2.198
        self.set_subtitle("But near tourist areas,", lang="en")
        self.wait(2.198)
        elapsed += 2.198
        self.set_subtitle("some drivers skip the meter and quote a", lang="en")
        self.wait(2.198)
        elapsed += 2.198
        self.set_subtitle("flat fare instead.", lang="en")
        self.wait(2.198)
        elapsed += 2.198
        self.set_subtitle("That's when you need this phrase:", lang="en")
        self.wait(2.198)
        elapsed += 2.198

        # Sync to 46.49s (l-0006)
        wait_gap = 46.49 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("เปิดมิเตอร์ด้วยค่ะ", lang="th", translit="bpèrt mí-dtêr dûuay khâ")
        self.show_thai_line("เปิดมิเตอร์ด้วยค่ะ", duration=3.0, pos="bottom", translit="bpèrt mí-dtêr dûuay khâ")
        elapsed += 3.0

        # Sync to 49.49s (l-0007)
        wait_gap = 49.49 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("That one phrase can save you over a", duration=2.508, lang="en")
        elapsed += 2.508
        self.set_subtitle("hundred baht.", lang="en")
        self.wait(2.508)
        elapsed += 2.508
        self.set_subtitle("Today I'm going to teach you everything you need for a Bangkok taxi", lang="en")
        self.wait(2.508)
        elapsed += 2.508
        self.set_subtitle("—", lang="en")
        self.wait(2.508)
        elapsed += 2.508
        self.set_subtitle("eight phrases that will make you look like", lang="en")
        self.wait(2.508)
        elapsed += 2.508
        self.set_subtitle("you know what you're doing.", lang="en")
        self.wait(2.508)
        elapsed += 2.508

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-003 (section-intro) ===
        # Sync to 65.04s (l-0008)
        wait_gap = 65.04 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("Let's do the vocabulary first.", lang="en")
        self.wait(1.610)
        elapsed += 1.610
        self.set_subtitle("Eight words.", lang="en")
        self.wait(1.610)
        elapsed += 1.610
        self.set_subtitle("For each one I'll explain what it means,", lang="en")
        self.wait(1.610)
        elapsed += 1.610
        self.set_subtitle("how to use it,", lang="en")
        self.wait(1.610)
        elapsed += 1.610
        self.set_subtitle("and give you a real example.", lang="en")
        self.wait(1.610)
        elapsed += 1.610
        self.set_subtitle("Let's go.", lang="en")
        self.wait(1.610)
        elapsed += 1.610

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-004 (vocab-card) ===
        # Sync to 75.20s (l-0009)
        wait_gap = 75.20 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "ไป",
            "to go",
            "bpai",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-005 (vocab-explain) ===
        # Sync to 78.20s (l-0010)
        wait_gap = 78.20 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ไป — bpai. This means 'to go'.", lang="en")
        self.wait(2.187)
        elapsed += 2.187
        self.set_subtitle("It's the first word out of your mouth when", lang="en")
        self.wait(2.187)
        elapsed += 2.187
        self.set_subtitle("you get in a taxi.", lang="en")
        self.wait(2.187)
        elapsed += 2.187
        self.set_subtitle("You just say ไป and then the destination —", lang="en")
        self.wait(2.187)
        elapsed += 2.187
        self.set_subtitle("that's it.", lang="en")
        self.wait(2.187)
        elapsed += 2.187
        self.set_subtitle("No grammar, no complicated structure.", lang="en")
        self.wait(2.187)
        elapsed += 2.187
        self.set_subtitle("ไปสยาม — go to Siam.", lang="en")
        self.wait(2.187)
        elapsed += 2.187
        self.set_subtitle("ไปสุขุมวิท — go to Sukhumvit.", lang="en")
        self.wait(2.187)
        elapsed += 2.187
        self.set_subtitle("ไปสนามบิน — go to the airport. Simple.", lang="en")
        self.wait(2.187)
        elapsed += 2.187

        # Sync to 97.88s (l-0011)
        wait_gap = 97.88 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("ไปสนามบินสุวรรณภูมิค่ะ", duration=3.0, pos="bottom", translit="bpai sa-nǎam-bin su-wan-na-phuum khâ")
        elapsed += 3.0

        # Sync to 100.88s (l-0012)
        wait_gap = 100.88 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'Go to Suvarnabhumi Airport,", lang="en")
        self.wait(2.282)
        elapsed += 2.282
        self.set_subtitle("please.' สนามบิน is airport.", lang="en")
        self.wait(2.282)
        elapsed += 2.282
        self.set_subtitle("สุวรรณภูมิ is the airport name.", lang="en")
        self.wait(2.282)
        elapsed += 2.282
        self.set_subtitle("ไป + destination + คะ —", lang="en")
        self.wait(2.282)
        elapsed += 2.282
        self.set_subtitle("that's your complete taxi instruction.", lang="en")
        self.wait(2.282)
        elapsed += 2.282

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-006 (vocab-card) ===
        # Sync to 112.79s (l-0013)
        wait_gap = 112.79 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "This next one is the phrase that will save you money.",
            "This next one is the phrase that will save you money.",
            "",
            duration=4.21,
        )
        elapsed += 4.21

        # Sync to 117.00s (l-0014)
        wait_gap = 117.00 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "เปิดมิเตอร์",
            "use the meter",
            "bpèrt mí-dtêr",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-007 (vocab-explain) ===
        # Sync to 120.00s (l-0015)
        wait_gap = 120.00 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("เปิดมิเตอร์ — bpèrt mí-dtêr.", lang="en")
        self.wait(2.190)
        elapsed += 2.190
        self.set_subtitle("'Use the meter' —", lang="en")
        self.wait(2.190)
        elapsed += 2.190
        self.set_subtitle("literally 'turn on the meter'.", lang="en")
        self.wait(2.190)
        elapsed += 2.190
        self.set_subtitle("เปิด means 'open' or 'turn on',", lang="en")
        self.wait(2.190)
        elapsed += 2.190
        self.set_subtitle("and มิเตอร์ is borrowed from English.", lang="en")
        self.wait(2.190)
        elapsed += 2.190
        self.set_subtitle("Say this as soon as you get in,", lang="en")
        self.wait(2.190)
        elapsed += 2.190
        self.set_subtitle("before you tell the driver where to go.", lang="en")
        self.wait(2.190)
        elapsed += 2.190
        self.set_subtitle("Most drivers will comply immediately.", lang="en")
        self.wait(2.190)
        elapsed += 2.190
        self.set_subtitle("If they refuse,", lang="en")
        self.wait(2.190)
        elapsed += 2.190
        self.set_subtitle("you just get out and find another taxi —", lang="en")
        self.wait(2.190)
        elapsed += 2.190
        self.set_subtitle("there are always more.", lang="en")
        self.wait(2.190)
        elapsed += 2.190

        # Sync to 144.09s (l-0016)
        wait_gap = 144.09 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("เปิดมิเตอร์ด้วยนะคะ", duration=3.0, pos="bottom", translit="bpèrt mí-dtêr dûuay ná khá")
        elapsed += 3.0

        # Sync to 147.09s (l-0017)
        wait_gap = 147.09 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'Please use the meter'.", lang="en")
        self.wait(2.450)
        elapsed += 2.450
        self.set_subtitle("ด้วย adds 'also' which softens it,", lang="en")
        self.wait(2.450)
        elapsed += 2.450
        self.set_subtitle("and นะ is a gentle particle.", lang="en")
        self.wait(2.450)
        elapsed += 2.450
        self.set_subtitle("Together they make it sound polite and", lang="en")
        self.wait(2.450)
        elapsed += 2.450
        self.set_subtitle("conversational rather than demanding.", lang="en")
        self.wait(2.450)
        elapsed += 2.450

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-008 (vocab-card) ===
        # Sync to 159.84s (l-0018)
        wait_gap = 159.84 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "Now — how to tell the driver to stop.",
            "Now — how to tell the driver to stop.",
            "",
            duration=3.09,
        )
        elapsed += 3.09

        # Sync to 162.93s (l-0019)
        wait_gap = 162.93 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "จอดตรงนี้",
            "stop here",
            "jàwt trong níi",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-009 (vocab-explain) ===
        # Sync to 165.93s (l-0020)
        wait_gap = 165.93 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("จอดตรงนี้ — jàwt trong níi.", lang="en")
        self.wait(1.935)
        elapsed += 1.935
        self.set_subtitle("This means 'stop here'.", lang="en")
        self.wait(1.935)
        elapsed += 1.935
        self.set_subtitle("จอด means 'stop' or 'park',", lang="en")
        self.wait(1.935)
        elapsed += 1.935
        self.set_subtitle("ตรงนี้ means 'right here'.", lang="en")
        self.wait(1.935)
        elapsed += 1.935
        self.set_subtitle("You say this when you're close to your", lang="en")
        self.wait(1.935)
        elapsed += 1.935
        self.set_subtitle("destination and want the driver to pull", lang="en")
        self.wait(1.935)
        elapsed += 1.935
        self.set_subtitle("over.", lang="en")
        self.wait(1.935)
        elapsed += 1.935
        self.set_subtitle("Add ด้วยค่ะ at the end — จอดตรงนี้ด้วยค่ะ", lang="en")
        self.wait(1.935)
        elapsed += 1.935
        self.set_subtitle("—", lang="en")
        self.wait(1.935)
        elapsed += 1.935
        self.set_subtitle("it's the standard polite version you'll", lang="en")
        self.wait(1.935)
        elapsed += 1.935
        self.set_subtitle("hear Thai people use.", lang="en")
        self.wait(1.935)
        elapsed += 1.935

        # Sync to 187.22s (l-0021)
        wait_gap = 187.22 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("จอดตรงหน้าร้านนั้นได้เลยค่ะ", duration=3.0, pos="bottom", translit="jàwt trong nâa ráan nán dâai loei khâ")
        elapsed += 3.0

        # Sync to 190.22s (l-0022)
        wait_gap = 190.22 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'You can stop right in front of that shop.' ตรงหน้า means 'right in front of',", lang="en")
        self.wait(3.098)
        elapsed += 3.098
        self.set_subtitle("ได้เลย means 'you can go ahead'.", lang="en")
        self.wait(3.098)
        elapsed += 3.098
        self.set_subtitle("A natural way to direct the driver to a", lang="en")
        self.wait(3.098)
        elapsed += 3.098
        self.set_subtitle("specific spot.", lang="en")
        self.wait(3.098)
        elapsed += 3.098

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-010 (vocab-card) ===
        # Sync to 203.11s (l-0023)
        wait_gap = 203.11 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "These next two help you direct the driver to exactly the right spot.",
            "These next two help you direct the driver to exactly the right spot.",
            "",
            duration=5.26,
        )
        elapsed += 5.26

        # Sync to 208.37s (l-0024)
        wait_gap = 208.37 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "ซ้ายมือ",
            "on the left side",
            "sáai muu",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-011 (vocab-explain) ===
        # Sync to 211.37s (l-0025)
        wait_gap = 211.37 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ซ้ายมือ — sáai muu.", lang="en")
        self.wait(1.904)
        elapsed += 1.904
        self.set_subtitle("'On the left side' —", lang="en")
        self.wait(1.904)
        elapsed += 1.904
        self.set_subtitle("literally 'left hand'.", lang="en")
        self.wait(1.904)
        elapsed += 1.904
        self.set_subtitle("ซ้าย is left, มือ is hand.", lang="en")
        self.wait(1.904)
        elapsed += 1.904
        self.set_subtitle("This tells the driver which side of the", lang="en")
        self.wait(1.904)
        elapsed += 1.904
        self.set_subtitle("road to stop on.", lang="en")
        self.wait(1.904)
        elapsed += 1.904
        self.set_subtitle("You'll say it once you're in the right", lang="en")
        self.wait(1.904)
        elapsed += 1.904
        self.set_subtitle("street and need to indicate the exact", lang="en")
        self.wait(1.904)
        elapsed += 1.904
        self.set_subtitle("side.", lang="en")
        self.wait(1.904)
        elapsed += 1.904
        self.set_subtitle("Combine it with จอด: จอดซ้ายมือค่ะ —", lang="en")
        self.wait(1.904)
        elapsed += 1.904
        self.set_subtitle("'stop on the left side'.", lang="en")
        self.wait(1.904)
        elapsed += 1.904

        # Sync to 232.31s (l-0026)
        wait_gap = 232.31 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("บ้านอยู่ซ้ายมือค่ะ", duration=3.0, pos="bottom", translit="bâan yùu sáai muu khâ")
        elapsed += 3.0

        # Sync to 235.31s (l-0027)
        wait_gap = 235.31 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'The house is on the left side.' บ้าน is house,", lang="en")
        self.wait(1.890)
        elapsed += 1.890
        self.set_subtitle("อยู่ is located.", lang="en")
        self.wait(1.890)
        elapsed += 1.890
        self.set_subtitle("Simple, clear.", lang="en")
        self.wait(1.890)
        elapsed += 1.890
        self.set_subtitle("The driver knows exactly where to pull", lang="en")
        self.wait(1.890)
        elapsed += 1.890
        self.set_subtitle("over.", lang="en")
        self.wait(1.890)
        elapsed += 1.890

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-012 (vocab-card) ===
        # Sync to 245.26s (l-0028)
        wait_gap = 245.26 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "And the other side.",
            "And the other side.",
            "",
            duration=2.5,
        )
        elapsed += 2.5

        # Sync to 247.76s (l-0029)
        wait_gap = 247.76 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "ขวามือ",
            "on the right side",
            "khwǎa muu",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-013 (vocab-explain) ===
        # Sync to 250.76s (l-0030)
        wait_gap = 250.76 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ขวามือ — khwǎa muu. 'On the right side'.", lang="en")
        self.wait(2.831)
        elapsed += 2.831
        self.set_subtitle("ขวา is right, มือ is hand.", lang="en")
        self.wait(2.831)
        elapsed += 2.831
        self.set_subtitle("ซ้ายมือ and ขวามือ are the pair —", lang="en")
        self.wait(2.831)
        elapsed += 2.831
        self.set_subtitle("left side and right side.", lang="en")
        self.wait(2.831)
        elapsed += 2.831
        self.set_subtitle("Once you know where you're going and which side of the road your destination is on,", lang="en")
        self.wait(2.831)
        elapsed += 2.831
        self.set_subtitle("these two words are all you need to land", lang="en")
        self.wait(2.831)
        elapsed += 2.831
        self.set_subtitle("the taxi exactly right.", lang="en")
        self.wait(2.831)
        elapsed += 2.831

        # Sync to 270.58s (l-0031)
        wait_gap = 270.58 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("ร้านอยู่ขวามือค่ะ ตึกสีขาวค่ะ", duration=3.0, pos="bottom", translit="ráan yùu khwǎa muu khâ dtèuk sǐi khǎao khâ")
        elapsed += 3.0

        # Sync to 273.58s (l-0032)
        wait_gap = 273.58 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'The shop is on the right side", lang="en")
        self.wait(2.422)
        elapsed += 2.422
        self.set_subtitle("—", lang="en")
        self.wait(2.422)
        elapsed += 2.422
        self.set_subtitle("the white building.' Adding a landmark like ตึกสีขาว", lang="en")
        self.wait(2.422)
        elapsed += 2.422
        self.set_subtitle("— white building — makes it much clearer.", lang="en")
        self.wait(2.422)
        elapsed += 2.422
        self.set_subtitle("Thai people always add a landmark.", lang="en")
        self.wait(2.422)
        elapsed += 2.422

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-014 (vocab-card) ===
        # Sync to 286.19s (l-0033)
        wait_gap = 286.19 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "Now — the payment question.",
            "Now — the payment question.",
            "",
            duration=2.5,
        )
        elapsed += 2.5

        # Sync to 288.69s (l-0034)
        wait_gap = 288.69 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "เท่าไหร่",
            "how much?",
            "thâo-rài",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-015 (vocab-explain) ===
        # Sync to 291.69s (l-0035)
        wait_gap = 291.69 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("เท่าไหร่ — thâo-rài.", lang="en")
        self.wait(2.235)
        elapsed += 2.235
        self.set_subtitle("'How much?' This is the most universal price question in Thai", lang="en")
        self.wait(2.235)
        elapsed += 2.235
        self.set_subtitle("— you'll use it in markets, restaurants,", lang="en")
        self.wait(2.235)
        elapsed += 2.235
        self.set_subtitle("taxis, everywhere.", lang="en")
        self.wait(2.235)
        elapsed += 2.235
        self.set_subtitle("In a taxi it tells the driver you're", lang="en")
        self.wait(2.235)
        elapsed += 2.235
        self.set_subtitle("checking the fare.", lang="en")
        self.wait(2.235)
        elapsed += 2.235
        self.set_subtitle("If the meter has been running,", lang="en")
        self.wait(2.235)
        elapsed += 2.235
        self.set_subtitle("the driver will just say the number.", lang="en")
        self.wait(2.235)
        elapsed += 2.235
        self.set_subtitle("Question word goes at the end —", lang="en")
        self.wait(2.235)
        elapsed += 2.235
        self.set_subtitle("เท่าไหร่คะ on its own is a complete", lang="en")
        self.wait(2.235)
        elapsed += 2.235
        self.set_subtitle("sentence.", lang="en")
        self.wait(2.235)
        elapsed += 2.235

        # Sync to 316.27s (l-0036)
        wait_gap = 316.27 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("ค่าโดยสารเท่าไหร่คะ", duration=3.0, pos="bottom", translit="khâa doi-sǎan thâo-rài khá")
        elapsed += 3.0

        # Sync to 319.27s (l-0037)
        wait_gap = 319.27 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'How much is the fare?'", lang="en")
        self.wait(2.572)
        elapsed += 2.572
        self.set_subtitle("ค่าโดยสาร is the formal word for fare.", lang="en")
        self.wait(2.572)
        elapsed += 2.572
        self.set_subtitle("You can also just say เท่าไหร่คะ and point at the meter", lang="en")
        self.wait(2.572)
        elapsed += 2.572
        self.set_subtitle("— totally normal.", lang="en")
        self.wait(2.572)
        elapsed += 2.572

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-016 (vocab-card) ===
        # Sync to 330.06s (l-0038)
        wait_gap = 330.06 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "Next — for when the price is too high.",
            "Next — for when the price is too high.",
            "",
            duration=3.16,
        )
        elapsed += 3.16

        # Sync to 333.22s (l-0039)
        wait_gap = 333.22 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "แพง",
            "expensive",
            "phaeng",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-017 (vocab-explain) ===
        # Sync to 336.22s (l-0040)
        wait_gap = 336.22 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("แพง — phaeng. 'Expensive'.", lang="en")
        self.wait(2.360)
        elapsed += 2.360
        self.set_subtitle("If a driver quotes you a flat rate that seems high,", lang="en")
        self.wait(2.360)
        elapsed += 2.360
        self.set_subtitle("say แพงไปค่ะ — 'that's too expensive'.", lang="en")
        self.wait(2.360)
        elapsed += 2.360
        self.set_subtitle("ไป here means 'excessively' or 'too much'.", lang="en")
        self.wait(2.360)
        elapsed += 2.360
        self.set_subtitle("It's polite, it's direct,", lang="en")
        self.wait(2.360)
        elapsed += 2.360
        self.set_subtitle("and every driver understands it as the", lang="en")
        self.wait(2.360)
        elapsed += 2.360
        self.set_subtitle("start of a negotiation.", lang="en")
        self.wait(2.360)
        elapsed += 2.360
        self.set_subtitle("Don't be shy about saying it —", lang="en")
        self.wait(2.360)
        elapsed += 2.360
        self.set_subtitle("bargaining is normal in Thailand,", lang="en")
        self.wait(2.360)
        elapsed += 2.360
        self.set_subtitle("even for taxis.", lang="en")
        self.wait(2.360)
        elapsed += 2.360

        # Sync to 359.82s (l-0041)
        wait_gap = 359.82 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("ร้อยห้าสิบบาทแพงไปหน่อยนะคะ", duration=3.0, pos="bottom", translit="ráwi-hâa-sìp bàat phaeng bpai nàuy ná khá")
        elapsed += 3.0

        # Sync to 362.82s (l-0042)
        wait_gap = 362.82 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'A hundred and fifty baht is a little expensive.' หน่อย and นะ soften it", lang="en")
        self.wait(2.572)
        elapsed += 2.572
        self.set_subtitle("— you're not angry,", lang="en")
        self.wait(2.572)
        elapsed += 2.572
        self.set_subtitle("you're just negotiating.", lang="en")
        self.wait(2.572)
        elapsed += 2.572
        self.set_subtitle("Very natural Thai.", lang="en")
        self.wait(2.572)
        elapsed += 2.572

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-018 (vocab-card) ===
        # Sync to 373.61s (l-0043)
        wait_gap = 373.61 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "And the follow-up — can you come down?",
            "And the follow-up — can you come down?",
            "",
            duration=3.16,
        )
        elapsed += 3.16

        # Sync to 376.77s (l-0044)
        wait_gap = 376.77 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_vocab_card(
            "ลดได้ไหม",
            "can you reduce (the price)?",
            "lót dâai mǎi",
            duration=2.5,
        )
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-019 (vocab-explain) ===
        # Sync to 379.77s (l-0045)
        wait_gap = 379.77 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ลดได้ไหม — lót dâai mǎi.", lang="en")
        self.wait(2.498)
        elapsed += 2.498
        self.set_subtitle("'Can you come down on the price?' ลด is 'reduce',", lang="en")
        self.wait(2.498)
        elapsed += 2.498
        self.set_subtitle("ได้ is 'can',", lang="en")
        self.wait(2.498)
        elapsed += 2.498
        self.set_subtitle("ไหม is the yes/no question particle.", lang="en")
        self.wait(2.498)
        elapsed += 2.498
        self.set_subtitle("You use this right after แพงไปค่ะ.", lang="en")
        self.wait(2.498)
        elapsed += 2.498
        self.set_subtitle("Together they're your negotiation combo.", lang="en")
        self.wait(2.498)
        elapsed += 2.498
        self.set_subtitle("Say แพงไปค่ะ, then ลดได้ไหมคะ —", lang="en")
        self.wait(2.498)
        elapsed += 2.498
        self.set_subtitle("the driver will either say yes and name a lower price,", lang="en")
        self.wait(2.498)
        elapsed += 2.498
        self.set_subtitle("or say no and you decide.", lang="en")
        self.wait(2.498)
        elapsed += 2.498

        # Sync to 402.25s (l-0046)
        wait_gap = 402.25 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_thai_line("สองร้อยบาทลดได้ไหมคะ", duration=3.0, pos="bottom", translit="sǎawng ráwi bàat lót dâai mǎi khá")
        elapsed += 3.0

        # Sync to 405.25s (l-0047)
        wait_gap = 405.25 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That means 'Can you bring two hundred baht down?' Leading with the price you know is fair shows you're informed,", lang="en")
        self.wait(3.943)
        elapsed += 3.943
        self.set_subtitle("not just a tourist.", lang="en")
        self.wait(3.943)
        elapsed += 3.943
        self.set_subtitle("That changes the dynamic completely.", lang="en")
        self.wait(3.943)
        elapsed += 3.943

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-020 (section-intro) ===
        # Sync to 417.58s (l-0048)
        wait_gap = 417.58 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("OK — eight phrases, all covered.", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("Now let's put them together in a full taxi ride", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("— getting in,", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("telling the driver where to go, directing,", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("and paying.", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("I'll speak at natural speed.", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("Don't pause —", lang="en")
        self.wait(2.030)
        elapsed += 2.030
        self.set_subtitle("just listen and see how much you catch.", lang="en")
        self.wait(2.030)
        elapsed += 2.030

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-021 (natural-listen) ===
        # Sync to 434.32s (l-0049)
        wait_gap = 434.32 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_accumulate("สวัสดีค่ะ ไปสยามค่ะ", duration=3.0, translit="sa-wàt-dii khâ bpai sa-yǎam khâ")
        elapsed += 3.0

        # Sync to 437.32s (l-0050)
        wait_gap = 437.32 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_accumulate("เปิดมิเตอร์ด้วยนะคะ", duration=3.0, translit="bpèrt mí-dtêr dûuay ná khá")
        elapsed += 3.0

        # Sync to 440.32s (l-0051)
        wait_gap = 440.32 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_accumulate("เลี้ยวซ้ายตรงนี้ค่ะ", duration=3.0, translit="líiaw sáai trong níi khâ")
        elapsed += 3.0

        # Sync to 443.32s (l-0052)
        wait_gap = 443.32 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_accumulate("จอดตรงนี้ด้วยค่ะ", duration=3.0, translit="jàwt trong níi dûuay khâ")
        elapsed += 3.0

        # Sync to 446.32s (l-0053)
        wait_gap = 446.32 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_accumulate("เท่าไหร่คะ", duration=3.0, translit="thâo-rài khá")
        elapsed += 3.0

        # Sync to 449.32s (l-0054)
        wait_gap = 449.32 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_accumulate("ขอบคุณค่ะ", duration=2.5, translit="khàawp-khun khâ")
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-022 (explain) ===
        # Sync to 452.32s (l-0055)
        wait_gap = 452.32 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("How was that?", duration=1.907, lang="en")
        elapsed += 1.907
        self.set_subtitle("Could you follow the conversation?", lang="en")
        self.wait(1.907)
        elapsed += 1.907
        self.set_subtitle("Now let's slow it right down and go", lang="en")
        self.wait(1.907)
        elapsed += 1.907
        self.set_subtitle("through it line by line.", lang="en")
        self.wait(1.907)
        elapsed += 1.907

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-023 (section-intro) ===
        # Sync to 460.45s (l-0056)
        wait_gap = 460.45 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("Same conversation —", lang="en")
        self.wait(2.240)
        elapsed += 2.240
        self.set_subtitle("but now I'll say each line, pause,", lang="en")
        self.wait(2.240)
        elapsed += 2.240
        self.set_subtitle("and explain exactly what each part means.", lang="en")
        self.wait(2.240)
        elapsed += 2.240

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-024 (breakdown) ===
        # Sync to 467.67s (l-0057)
        wait_gap = 467.67 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_breakdown_triplet(
            "ไปสยามค่ะ",
            "bpai sa-yǎam khâ",
            "Go to Siam, please.",
            duration=6.5,
            translit_delay=3.0,
            english_delay=5.0,
        )
        elapsed += 6.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-025 (explain) ===
        # Sync to 474.67s (l-0060)
        wait_gap = 474.67 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("ไป is 'go'.", duration=2.021, lang="en")
        elapsed += 2.021
        self.set_subtitle("สยาม is Siam —", lang="en")
        self.wait(2.021)
        elapsed += 2.021
        self.set_subtitle("the area around Siam Paragon and MBK in", lang="en")
        self.wait(2.021)
        elapsed += 2.021
        self.set_subtitle("central Bangkok.", lang="en")
        self.wait(2.021)
        elapsed += 2.021
        self.set_subtitle("ค่ะ is the polite particle for women.", lang="en")
        self.wait(2.021)
        elapsed += 2.021
        self.set_subtitle("No verb 'to take me to', no 'please' word", lang="en")
        self.wait(2.021)
        elapsed += 2.021
        self.set_subtitle("— just ไป, destination, ค่ะ.", lang="en")
        self.wait(2.021)
        elapsed += 2.021
        self.set_subtitle("That's the complete, natural sentence.", lang="en")
        self.wait(2.021)
        elapsed += 2.021

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-026 (breakdown) ===
        # Sync to 491.34s (l-0061)
        wait_gap = 491.34 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_breakdown_triplet(
            "เปิดมิเตอร์ด้วยนะคะ",
            "bpèrt mí-dtêr dûuay ná khá",
            "Please use the meter.",
            duration=6.5,
            translit_delay=3.0,
            english_delay=5.0,
        )
        elapsed += 6.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-027 (explain) ===
        # Sync to 498.34s (l-0064)
        wait_gap = 498.34 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("เปิดมิเตอร์ — turn on the meter.", duration=2.240, lang="en")
        elapsed += 2.240
        self.set_subtitle("ด้วย adds 'also / as well',", lang="en")
        self.wait(2.240)
        elapsed += 2.240
        self.set_subtitle("นะ is a softening particle.", lang="en")
        self.wait(2.240)
        elapsed += 2.240
        self.set_subtitle("Together they make it sound friendly", lang="en")
        self.wait(2.240)
        elapsed += 2.240
        self.set_subtitle("rather than confrontational.", lang="en")
        self.wait(2.240)
        elapsed += 2.240
        self.set_subtitle("The driver hears this and clicks the meter on", lang="en")
        self.wait(2.240)
        elapsed += 2.240
        self.set_subtitle("— and you're protected.", lang="en")
        self.wait(2.240)
        elapsed += 2.240

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-028 (breakdown) ===
        # Sync to 514.52s (l-0065)
        wait_gap = 514.52 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_breakdown_triplet(
            "จอดตรงนี้ด้วยค่ะ",
            "jàwt trong níi dûuay khâ",
            "Please stop here.",
            duration=6.5,
            translit_delay=3.0,
            english_delay=5.0,
        )
        elapsed += 6.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-029 (explain) ===
        # Sync to 521.52s (l-0068)
        wait_gap = 521.52 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("จอด is 'stop' or 'park'.", duration=1.873, lang="en")
        elapsed += 1.873
        self.set_subtitle("ตรงนี้ is 'right here'.", lang="en")
        self.wait(1.873)
        elapsed += 1.873
        self.set_subtitle("ด้วยค่ะ makes it polite.", lang="en")
        self.wait(1.873)
        elapsed += 1.873
        self.set_subtitle("You'd say this while pointing or gesturing", lang="en")
        self.wait(1.873)
        elapsed += 1.873
        self.set_subtitle("to exactly where you want to be dropped", lang="en")
        self.wait(1.873)
        elapsed += 1.873
        self.set_subtitle("off.", lang="en")
        self.wait(1.873)
        elapsed += 1.873
        self.set_subtitle("Short and clear —", lang="en")
        self.wait(1.873)
        elapsed += 1.873
        self.set_subtitle("the driver pulls over immediately.", lang="en")
        self.wait(1.873)
        elapsed += 1.873

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-030 (breakdown) ===
        # Sync to 537.00s (l-0069)
        wait_gap = 537.00 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_breakdown_triplet(
            "เท่าไหร่คะ",
            "thâo-rài khá",
            "How much?",
            duration=6.5,
            translit_delay=3.0,
            english_delay=5.0,
        )
        elapsed += 6.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-031 (explain) ===
        # Sync to 544.00s (l-0072)
        wait_gap = 544.00 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_subtitle("เท่าไหร่คะ — how much?", duration=1.750, lang="en")
        elapsed += 1.750
        self.set_subtitle("The question word at the end,", lang="en")
        self.wait(1.750)
        elapsed += 1.750
        self.set_subtitle("just like อยู่ที่ไหน from the last", lang="en")
        self.wait(1.750)
        elapsed += 1.750
        self.set_subtitle("episode.", lang="en")
        self.wait(1.750)
        elapsed += 1.750
        self.set_subtitle("If the meter has been running,", lang="en")
        self.wait(1.750)
        elapsed += 1.750
        self.set_subtitle("the driver will point at it or say the", lang="en")
        self.wait(1.750)
        elapsed += 1.750
        self.set_subtitle("number.", lang="en")
        self.wait(1.750)
        elapsed += 1.750
        self.set_subtitle("You pay the exact meter price —", lang="en")
        self.wait(1.750)
        elapsed += 1.750
        self.set_subtitle("no tipping needed.", lang="en")
        self.wait(1.750)
        elapsed += 1.750

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-032 (section-intro) ===
        # Sync to 560.25s (l-0073)
        wait_gap = 560.25 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("Let's practise.", lang="en")
        self.wait(1.802)
        elapsed += 1.802
        self.set_subtitle("I'll give you a situation —", lang="en")
        self.wait(1.802)
        elapsed += 1.802
        self.set_subtitle("you say the Thai.", lang="en")
        self.wait(1.802)
        elapsed += 1.802
        self.set_subtitle("Say it out loud before I give the answer.", lang="en")
        self.wait(1.802)
        elapsed += 1.802

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-033 (drill-prompt) ===
        # Sync to 567.96s (l-0074)
        wait_gap = 567.96 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_drill_prompt(
            "You get in a taxi. How do you say: 'Go to Sukhumvit, please'?",
            duration=6.27,
            try_delay=4.77,
        )
        elapsed += 6.27

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-034 (drill-answer) ===
        # Sync to 574.73s (l-0076)
        wait_gap = 574.73 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ไปสุขุมวิทค่ะ", lang="th", translit="bpai su-khǔm-wít khâ")
        self.show_thai_line("ไปสุขุมวิทค่ะ", duration=3.0, fade_in=False, translit="bpai su-khǔm-wít khâ")
        elapsed += 3.0

        # Sync to 577.73s (l-0077)
        wait_gap = 577.73 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That's right —", lang="en")
        self.wait(1.75)
        elapsed += 1.75
        self.set_subtitle("ไปสุขุมวิท means 'go to Sukhumvit'.", lang="en")
        self.wait(1.75)
        elapsed += 1.75

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-035 (drill-prompt) ===
        # Sync to 581.73s (l-0078)
        wait_gap = 581.73 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_drill_prompt(
            "The driver wants to charge you a flat rate. How do you say: 'Please use the meter'?",
            duration=7.81,
            try_delay=6.31,
        )
        elapsed += 7.81

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-036 (drill-answer) ===
        # Sync to 590.04s (l-0080)
        wait_gap = 590.04 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("เปิดมิเตอร์ด้วยค่ะ", lang="th", translit="bpèrt mí-dtêr dûuay khâ")
        self.show_thai_line("เปิดมิเตอร์ด้วยค่ะ", duration=3.0, fade_in=False, translit="bpèrt mí-dtêr dûuay khâ")
        elapsed += 3.0

        # Sync to 593.04s (l-0081)
        wait_gap = 593.04 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That's right —", lang="en")
        self.wait(1.47)
        elapsed += 1.47
        self.set_subtitle("เปิดมิเตอร์ด้วยค่ะ means 'please use the", lang="en")
        self.wait(1.47)
        elapsed += 1.47
        self.set_subtitle("meter'.", lang="en")
        self.wait(1.47)
        elapsed += 1.47

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-037 (drill-prompt) ===
        # Sync to 597.95s (l-0082)
        wait_gap = 597.95 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_drill_prompt(
            "You've arrived. How do you say: 'Please stop here'?",
            duration=5.57,
            try_delay=4.07,
        )
        elapsed += 5.57

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-038 (drill-answer) ===
        # Sync to 604.02s (l-0084)
        wait_gap = 604.02 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("จอดตรงนี้ด้วยค่ะ", lang="th", translit="jàwt trong níi dûuay khâ")
        self.show_thai_line("จอดตรงนี้ด้วยค่ะ", duration=3.0, fade_in=False, translit="jàwt trong níi dûuay khâ")
        elapsed += 3.0

        # Sync to 607.02s (l-0085)
        wait_gap = 607.02 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("That's right —", lang="en")
        self.wait(1.995)
        elapsed += 1.995
        self.set_subtitle("จอดตรงนี้ด้วยค่ะ means 'please stop here'.", lang="en")
        self.wait(1.995)
        elapsed += 1.995

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-039 (section-intro) ===
        # Sync to 611.51s (l-0086)
        wait_gap = 611.51 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("Last section — shadowing.", lang="en")
        self.wait(1.680)
        elapsed += 1.680
        self.set_subtitle("Repeat each phrase after me.", lang="en")
        self.wait(1.680)
        elapsed += 1.680
        self.set_subtitle("Say it out loud.", lang="en")
        self.wait(1.680)
        elapsed += 1.680
        self.set_subtitle("This is where your pronunciation gets", lang="en")
        self.wait(1.680)
        elapsed += 1.680
        self.set_subtitle("locked in.", lang="en")
        self.wait(1.680)
        elapsed += 1.680

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-040 (shadowing) ===
        # Sync to 620.41s (l-0087)
        wait_gap = 620.41 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ไป สยาม ค่ะ", lang="th", translit="bpai sa-yǎam khâ")
        self.show_shadowing_line("ไป สยาม ค่ะ", duration=3.0, highlight=True, translit="bpai sa-yǎam khâ")
        elapsed += 3.0

        # Sync to 623.41s (l-0088)
        wait_gap = 623.41 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("เปิด มิเตอร์ ด้วย ค่ะ", lang="th", translit="bpèrt mí-dtêr dûuay khâ")
        self.show_shadowing_line("เปิด มิเตอร์ ด้วย ค่ะ", duration=3.0, highlight=True, translit="bpèrt mí-dtêr dûuay khâ")
        elapsed += 3.0

        # Sync to 626.41s (l-0089)
        wait_gap = 626.41 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("เลี้ยว ซ้าย ตรง นี้ ค่ะ", lang="th", translit="líiaw sáai trong níi khâ")
        self.show_shadowing_line("เลี้ยว ซ้าย ตรง นี้ ค่ะ", duration=3.0, highlight=True, translit="líiaw sáai trong níi khâ")
        elapsed += 3.0

        # Sync to 629.41s (l-0090)
        wait_gap = 629.41 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("จอด ตรง นี้ ด้วย ค่ะ", lang="th", translit="jàwt trong níi dûuay khâ")
        self.show_shadowing_line("จอด ตรง นี้ ด้วย ค่ะ", duration=3.0, highlight=True, translit="jàwt trong níi dûuay khâ")
        elapsed += 3.0

        # Sync to 632.41s (l-0091)
        wait_gap = 632.41 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("เท่า ไหร่ คะ", lang="th", translit="thâo-rài khá")
        self.show_shadowing_line("เท่า ไหร่ คะ", duration=3.0, highlight=True, translit="thâo-rài khá")
        elapsed += 3.0

        # Sync to 635.41s (l-0092)
        wait_gap = 635.41 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("ขอบ คุณ ค่ะ", lang="th", translit="khàawp-khun khâ")
        self.show_shadowing_line("ขอบ คุณ ค่ะ", duration=2.5, highlight=True, translit="khàawp-khun khâ")
        elapsed += 2.5

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-041 (recap) ===
        # Sync to 638.41s (l-0093)
        wait_gap = 638.41 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_english_line("Quick recap — here are your 8 taxi phrases in Thai.", duration=3.57)
        elapsed += 3.57

        # --- Block transition ---
        self.snap_clear()

        # === Block: b-042 (teaser) ===
        # Sync to 642.48s (l-0094)
        wait_gap = 642.48 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.show_english_line("Next time — shopping at the Thai market. You'll learn how to ask the price, haggle, and say 'too expensive'. See you then.", duration=9.04)
        elapsed += 9.04

        # Sync to 651.52s (l-0095)
        wait_gap = 651.52 - elapsed
        if wait_gap > 0.02:
            self.wait(wait_gap)
            elapsed += wait_gap

        self.set_subtitle("แพงไปค่ะ", lang="th", translit="phaeng bpai khâ")
        self.show_thai_line("แพงไปค่ะ", duration=2.5, fade_in=True, translit="phaeng bpai khâ")
        elapsed += 2.5

